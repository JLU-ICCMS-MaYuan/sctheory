interface:
  display_name: "科研 PDF 拆解"
  short_description: "仅用本地轻量工具拆解用户提供的科研 PDF"
  default_prompt: "Use $ingest-research-pdf to extract only the PDF I provide with pypdf, pdfplumber, and Poppler, without network retrieval or OCR models."
policy:
  allow_implicit_invocation: true
