## 2006 MgB₂ 紧束缚模型记录（Codex 工作笔记）

- **文本提取**：尝试 `pdftotext`、`mutool`、`gs`、`soffice` 均因缺少依赖失败，改用 Python 解压 PDF 流并整理生成 `2006_MgB2_extracted.txt` 与清洗版 `2006_MgB2_extracted_clean.txt`，后者便于搜索公式与参数。
- **文章解读**：整理了文献核心思想与结果，关注两带 BCS、正交紧束缚模型、薄膜量子化、电子外溢参数 Λ，以及体相 DOS 与耦合常数的取值。
- **Python 脚本**：首次版本因未考虑电子数守恒与 DOS 归一导致结果失真，已弃用。现保留 `reproduce_mgb2_tba.py`，包含：
  - 在第一布里渊区蒙特卡洛采样 π、σ 能带（σ 仍为文中 A 点附近抛物线近似）。
  - 二分搜索费米能，确保总电子数 nₑ≈2.9，并输出各子带占据。
  - 在费米面附近估算单自旋 DOS，并用 Δσ(0)=7.1 meV、Δπ(0)=2.80 meV、Tc=37.6 K 反解 λ=V·g。
- **当前偏差**：由于 σ 带采用抛物线简化，估算的 gσ、gπ 远低于文献值（≈0.28 / 0.15 eV⁻¹），导致求得的 V_ij 畸大。需在下一步改回正交紧束缚 3×3 哈密顿量（对应原文式(20)）再数值对角化。
- **后续提醒**：
  1. 重新实现 σ 带的完整紧束缚矩阵后，再运行脚本验证 DOS 与 λ。
  2. 若要扩展到薄膜，需要引入离散 k_z、边界相位因子及 Λ≠0 的几何因子。
  3. 当前脚本默认采样 2×10⁵ 点，可通过参数 `-n` 调整；注意增加样本以降低 DOS 噪声。

